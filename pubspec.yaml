name: cars_market
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.0
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  theme:
    path: packages/core/theme
  constants:
    path: packages/core/constants
  google_places_service:
    path: packages/core/google_places_service
  router:
    path: packages/core/router
  on_bording:
    path: packages/features/on_bording
  create_account:
    path: packages/features/auth/create_account
  login:
    path: packages/features/auth/login
  user_details:
    path: packages/features/auth/user_details
  forgot_password:
    path: packages/features/auth/forgot_password
  add:
    path: packages/features/home/add
  remote:
    path: packages/core/remote
  supabase_flutter: ^2.10.1
  get_it: ^8.2.0
  injectable: ^2.5.1
  flutter_dotenv: ^6.0.0
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1

dev_dependencies:
  melos: ^7.1.1
  injectable_generator: ^2.8.1
  build_runner: ^2.7.1
workspace:
  - packages/core/theme
  - packages/core/data
  - packages/core/domain
  - packages/core/constants
  - packages/core/router
  - packages/core/remote
  - packages/core/assets
  - packages/core/shared_ui
  - packages/core/error_handler
  - packages/core/google_places_service
  - packages/features/on_bording
  - packages/features/auth/login
  - packages/features/auth/user_details
  - packages/features/auth/create_account
  - packages/features/auth/forgot_password
  - packages/features/home/home
  - packages/features/home/main
  - packages/features/home/search
  - packages/features/home/add
  - packages/features/home/favorites
  - packages/features/home/profile

melos:
  scripts:
    clean: melos clean
    runner: flutter pub run build_runner build --delete-conflicting-outputs
    clean and bootstrap: melos clean && melos bootstrap
    test:
      run: melos exec --concurrency=1 -- flutter test

    update: flutter pub outdated
    upgrade: flutter pub upgrade --major-versions
  command:
    bootstrap:
      environment:
        sdk: ^3.9.0
      dependencies:
        smooth_page_indicator: ^1.2.1
        flutter_bloc: ^9.1.1
        go_router: ^16.2.4
        supabase_flutter: ^2.10.1
        get_it: ^8.2.0
        injectable: ^2.5.1
        connectivity_plus: ^7.0.0
        dartz: ^0.10.1
        another_flushbar: ^1.12.32
        get: ^4.7.2
        iconsax_flutter: ^1.0.1
        intl: ^0.20.2
        image_picker: ^1.2.0
        photo_view: ^0.15.0
        reorderables: ^0.6.0
        swipe_image_gallery: ^0.11.0
        photo_viewer: ^0.1.0
        widget_zoom: ^0.0.4
        uuid: ^4.5.1
        lottie: ^3.3.2
        flutter_animate: ^4.5.2
        geolocator: ^14.0.2
        google_maps_flutter: ^2.13.1
        flutter_google_places_sdk: ^0.4.2+1
        googleapis: ^15.0.0
        dio: ^5.9.0
        pretty_dio_logger: ^1.4.0
        flutter_dotenv: ^6.0.0
        intl_phone_number_input: ^0.7.5
        pinput: ^5.0.2
        firebase_core: ^4.2.0
        firebase_auth: ^6.1.1
        redacted: ^1.0.13

      dev_dependencies:
        injectable_generator: ^2.8.1
        build_runner: ^2.7.1
        dotted_border: ^2.0.0

  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
  # - assets/images/zekrak.png
  # - assets/images/We.jpg
  # - assets/images/UMM.jpg
  # - assets/images/UM.jpg
  # - assets/svg/api.svg
  # - assets/svg/dart.svg
  # - assets/svg/firebase.svg
  # - assets/svg/flutter.svg
  # - assets/svg/git.svg
  # - assets/svg/sql.svg
  # - assets/svg/postman.svg
  # - assets/svg/subabase.svg
  # - assets/svg/apple.svg
  # - assets/svg/android.svg
  # - assets/svg/desktop.svg
  # - assets/svg/web.svg
  # - assets/svg/multi.svg
  # - assets/svg/clean.svg
  # - assets/svg/github.svg
  # - assets/svg/apple-store.svg
  # - assets/svg/google-play.svg
  # - assets/svg/email.svg
  # - assets/svg/phone.svg
  # - assets/svg/linkdin.svg
  # - assets/svg/location.svg
  # - assets/svg/whatsapp.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/inter/Inter-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/inter/Inter-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/inter/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/inter/Inter-Regular.ttf
  #         weight: 400

  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/poppins/Poppins-Black.ttf
  #         weight: 900
  #       - asset: assets/fonts/poppins/Poppins-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/poppins/Poppins-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/poppins/Poppins-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/poppins/Poppins-Regular.ttf
  #         weight: 400
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
